python3 -m venv venv
venv\Scripts\activate
pip install sanic sanic-ext jinja2 asyncpg
pip install sanic-session
pip install aiomysql
pip freeze > requirements.txt
pip install -r requirements.txt


-----------------
En el mysql ponemos 
ALTER TABLE usuarios ADD COLUMN password VARCHAR(100) NOT NULL;
INSERT INTO usuarios (usuario, descripcion, password)
VALUES ('admin', 'Administrador del sistema', 'adminpass');



mysql -u admin -p incidencias < init_db.sql